export type LoginFormValues = {
    email: string;
    password: string;
};

export type RegistrationFormValues = {
    first_name: string;
    last_name: string;
    email: string;
    password: string;
};