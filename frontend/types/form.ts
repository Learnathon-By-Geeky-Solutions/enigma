export type LoginFormValues = {
    email: string;
    password: string;
};

export type RegistrationFormValues = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};